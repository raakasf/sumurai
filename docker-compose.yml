name: sumurai

# Network topology:
#   app_net    — internal: true (no internet route); all services communicate here.
#   egress_net — has internet access; only squid sits on this network.
#   public_net — exposes nginx host ports (8080/8443). Nginx applies an
#                in-container OUTPUT firewall before start so it can serve
#                inbound traffic without initiating internet egress.
#
# The backend has no internet route of its own. Any outbound HTTPS must go
# through squid (HTTPS_PROXY), which whitelists only Teller, Plaid, and
# Frankfurter exchange-rate endpoints.
networks:
  app_net:
    internal: true
  egress_net: {}
  public_net: {}

services:
  frontend:
    image: ${SUMURAI_GHCR_PREFIX:-ghcr.io/twobitfoundry/sumurai}-frontend:${SUMURAI_IMAGE_TAG:-latest}
    pull_policy: always
    environment:
      NEXT_PUBLIC_OTEL_ENABLED: "false"
      NEXT_PUBLIC_OTEL_SERVICE_NAME: sumurai-frontend
      NEXT_PUBLIC_OTEL_SERVICE_VERSION: "1.0.0"
      NEXT_PUBLIC_OTEL_CAPTURE_BODIES: "false"
      NEXT_PUBLIC_OTEL_BLOCK_SENSITIVE_ENDPOINTS: "true"
    depends_on:
      - backend
    healthcheck:
      test: [ "CMD", "wget", "-q", "-O-", "http://127.0.0.1:8080/health" ]
      interval: 15s
      timeout: 5s
      retries: 3

  backend:
    image: ${SUMURAI_GHCR_PREFIX:-ghcr.io/twobitfoundry/sumurai}-backend:${SUMURAI_IMAGE_TAG:-latest}
    pull_policy: always
    environment:
      REDIS_URL: redis://redis:6379
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/${POSTGRES_DB:-accounting}
      POSTGRES_DB: ${POSTGRES_DB:-accounting}
      MIGRATION_ARTIFACTS_DIR: /var/lib/sumurai/migration

      AUTH_COOKIE_SAME_SITE: Strict
      APP_ORIGIN: ${APP_ORIGIN:-http://localhost:8080}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:8080},${NGROK_URL:-}

      OTEL_TRACES_EXPORTER: none

      TELLER_APPLICATION_ID: ${TELLER_APPLICATION_ID}
      TELLER_CERT_PATH: /etc/teller/certificate.pem
      TELLER_KEY_PATH: /etc/teller/private_key.pem
      TELLER_ENV: development

      PLAID_CLIENT_ID: ${PLAID_CLIENT_ID}
      PLAID_SECRET: ${PLAID_SECRET}
      PLAID_ENV: production

      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY is required}
      SEED_DEMO_USER: "true"
      RUST_LOG: info,otel::tracing=trace,axum_tracing_opentelemetry=trace
    volumes:
      - .certs/teller/certificate.pem:/etc/teller/certificate.pem:ro
      - .certs/teller/private_key.pem:/etc/teller/private_key.pem:ro
      - migration_artifacts:/var/lib/sumurai/migration
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:3000/health" ]
      interval: 10s
      timeout: 5s
      retries: 5

  squid:
    image: ubuntu/squid:latest
    platform: linux/amd64
    restart: unless-stopped
    volumes:
      - ./squid/squid.conf:/etc/squid/squid.conf:ro
    networks:
      - app_net
      - egress_net

  redis:
    image: redis:7-alpine
    platform: linux/amd64
    command: redis-server --appendonly yes
    restart: unless-stopped
    networks:
      - app_net
    volumes:
      - redis_data_oss:/data

  postgres:
    image: postgres:15-alpine
    platform: linux/amd64
    environment:
      POSTGRES_DB: accounting
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
    volumes:
      - postgres_data_oss:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d accounting" ]
      interval: 5s
      timeout: 5s
      retries: 5

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    platform: linux/amd64
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy
    environment:
      DOMAIN: ${DOMAIN:-localhost}
      SSL_PORT: ${SSL_PORT:-8443}
      LOCAL_HTTPS_CERT_DIR: /etc/nginx/local-certs
      NGINX_EGRESS_LOCKDOWN: ${NGINX_EGRESS_LOCKDOWN:-true}
    command: [ "/bin/sh", "/etc/nginx/entrypoint.sh" ]
    cap_add:
      - NET_ADMIN
    ports:
      - "${HTTP_PORT:-8080}:80"
      - "${HTTPS_PORT:-8443}:443"
    volumes:
      - ./nginx/nginx.slim.conf.template:/etc/nginx/nginx.conf.template:ro
      - ./nginx/entrypoint.sh:/etc/nginx/entrypoint.sh:ro
      - ./nginx/healthcheck.sh:/etc/nginx/healthcheck.sh:ro
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/www/certbot
    healthcheck:
      test: [ "CMD", "/bin/sh", "/etc/nginx/healthcheck.sh" ]
      interval: 10s
      timeout: 5s
      retries: 3

  certbot:
    image: certbot/certbot:latest
    pull_policy: always
    profiles: [ "certbot" ]
    restart: unless-stopped
    # certbot needs internet access to reach Let's Encrypt ACME servers.
    networks:
      - egress_net
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/www/certbot
    entrypoint: [ "sh", "-c", "sleep infinity" ]

  ngrok:
    image: ngrok/ngrok:latest
    pull_policy: always
    profiles:
      - ngrok
    depends_on:
      nginx:
        condition: service_healthy
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN:-}
      NGROK_URL: ${NGROK_URL:-}
    entrypoint:
      - /bin/sh
      - -c
    command:
      - |
        if [ -n "$$NGROK_URL" ]; then
          exec ngrok http --url="$$NGROK_URL" https://nginx:443
        fi
        exec ngrok http https://nginx:443

volumes:
  redis_data_oss:
  postgres_data_oss:
  migration_artifacts:
  certbot-etc:
  certbot-var:
