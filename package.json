{
  "name": "sumurai",
  "version": "6.4.0",
  "private": true,
  "engines": {
    "node": ">=24.10.0",
    "bun": ">=1.3.14"
  },
  "scripts": {
    "prepare": "husky",
    "rust:fmt": "cargo fmt -p sumurai-backend -p entity -p sumurai-cli --check",
    "rust:clippy": "cargo clippy -p sumurai-backend -p entity -p sumurai-cli --locked --all-targets --no-deps -- -D warnings",
    "rust:lint": "bun run rust:fmt && bun run rust:clippy",
    "rust:typecheck": "cargo check --workspace --locked --all-targets",
    "rust:test": "cargo test -p sumurai-backend --locked",
    "rust:test:ci": "RUST_BACKTRACE=1 cargo test -p sumurai-backend -p sumurai-cli --locked && cargo test -p sumurai-backend --locked --features dev-seed seed_password",
    "backend:ci": "bun run rust:fmt && bun run rust:typecheck && bun run rust:clippy && bun run rust:test:ci",
    "frontend:typecheck": "bun --cwd=frontend run typecheck",
    "frontend:lint": "bun --cwd=frontend run lint",
    "frontend:test": "bun --cwd=frontend test",
    "frontend:design": "bun --cwd=frontend run design:guard",
    "frontend:build": "bun --cwd=frontend run build",
    "frontend:storybook-runtime:run": "bun --cwd=frontend run test:storybook-runtime:run",
    "frontend:storybook-test": "bun --cwd=frontend run test:storybook",
    "frontend:playwright-install": "bun --cwd=frontend run playwright:install",
    "frontend:ci": "bun --cwd=frontend ci && bun --cwd=frontend run lint && bun --cwd=frontend run typecheck && bun --cwd=frontend run design:guard && bun --cwd=frontend run test:ci && bun --cwd=frontend run build && bun --cwd=frontend run playwright:install-ci && bun --cwd=frontend run test:storybook && bun --cwd=frontend run storybook:build && bun --cwd=frontend run test:storybook-runtime:run",
    "test:storybook-runtime": "bun --cwd=frontend run test:storybook-runtime",
    "storybook": "bun --cwd=frontend run storybook",
    "storybook:build": "bun --cwd=frontend run storybook:build",
    "precommit": "bun --cwd=frontend run precommit && bun run backend:ci",
    "release": "semantic-release"
  },
  "devDependencies": {
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.8",
    "@semantic-release/release-notes-generator": "^14.1.1",
    "conventional-changelog-conventionalcommits": "^9.3.1",
    "husky": "^9.1.7",
    "semantic-release": "^25.0.3"
  }
}
