name: sumurai

services:
  nginx:
    environment:
      DOMAIN: ${DOMAIN:?DOMAIN is required for production}
      ENVIRONMENT: production
      SSL_PORT: 443
    ports: !override
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
